# OKLCH 무채색(Neutral) 팔레트 제너레이터 스펙 및 기능 설명서

본 문서는 브랜드 주 색상(Primary Color)과의 조화로운 UI 디자인을 지원하기 위해 구축된 **OKLCH 무채색(Neutral) 팔레트 제너레이터(Neutral Palette Generator)**의 핵심 개념, 보정 알고리즘, 주요 기능 및 기술 스펙을 정리한 가이드 문서입니다.

---

## 🌟 1. 서비스 개요 (Overview)

**OKLCH 무채색 제너레이터**는 UI 디자인 시스템의 뼈대가 되는 **무채색 스케일(Slate, Cool Gray, True Neutral, Warm Gray)**을 인간의 실제 시각 인지 흐름에 부합하는 **OKLCH 공간**에서 정밀하게 생성하는 도구입니다.

브랜드 주요 메인 컬러(Primary Base Color)를 분석하여 가장 어울리는 무채색 톤을 지능적으로 제안하는 **오토 페어링(Auto-pairing)**을 탑재하고 있으며, Tailwind CSS v4.3 최신 무채색 규격에 맞춘 **11단계(50~950) 스케일 확장 및 실측 정규화 S-Curve명도/종형 채도 곡선 파이프라인**을 적용했습니다. 또한 웹 접근성(WCAG 2.1) 명암비를 동적으로 연산하고, 피그마 Variables 다중 모드(Light / Dark) 임포트를 위한 네이티브 JSON 스펙을 안전하게 지원합니다.

---

## ⚙️ 2. 핵심 물리/수학적 보정 및 매핑 엔진

본 제너레이터는 UI 화면에서 문자의 배경이나 기본 뼈대 요소로 사용되는 무채색의 특성을 극대화하기 위해 다음과 같은 특화 알고리즘이 가동됩니다.

### ① Tailwind Slate 실측 정규화 S-Curve 명도 매핑 (`lOffsets`)
* **메커니즘**: 단순 직선형 선형 보간이나 단일 거듭제곱 공식을 폐기하고, Tailwind Slate 명도 수치에서 100% 정규화 추출된 11단계 하향 비율 배열(`lOffsets`)을 도입했습니다.
  * `const lOffsets = [0.000, 0.019, 0.065, 0.135, 0.328, 0.503, 0.630, 0.716, 0.825, 0.908, 1.000];`
  * `lVal = state.maxL - (state.maxL - state.minL) * lOffsets[index]`
* **효과**: 슬라이더로 `Max L`(기본값 0.98 / 98.0%)과 `Min L`(기본값 0.08 / 8.0%)을 조절하면, 50~200단계 상단 구간은 미세하게 떨어지면서 500단계(index 5)는 상하단 명도 차이의 50.3% 지점인 **L 52.7% 부근에 완벽 안착**하여 중간~하단 스케일이 허얗게 뜨는 현상을 원천 방지합니다.

### ② 500단계 피크 종형 채도 포물선 곡선 (`cRatios`) & 톤별 Peak Chroma 매핑
* **메커니즘**: Tailwind CSS 무채색 실측 데이터에 기반하여 500단계에서 채도가 가장 또렷하게 살고, 양극단인 50단계(파스텔 배경)와 950단계(딥 톤 무채색)로 갈수록 채도가 대폭 감소하는 정규화 종형 비율 배열(`cRatios`)과 톤별 Peak Chroma 매핑을 주입했습니다.
  * `const cRatios = [0.07, 0.15, 0.30, 0.55, 0.85, 1.00, 0.90, 0.75, 0.50, 0.30, 0.15];`
  * **Slate**: `maxChroma = 0.046` (500단계 C = 0.0460 피크, 50단계 C = 0.0032)
  * **Cool**: `maxChroma = 0.027` (500단계 C = 0.0270 피크, 50단계 C = 0.0019)
  * **Warm**: `maxChroma = 0.013` (500단계 C = 0.0130 피크, 50단계 C = 0.0009)
  * **Neutral**: `maxChroma = 0.000` (전 구간 C = 0.0000)

### ③ 지능형 오토 페어링 엔진 (Auto-pairing)
* **메커니즘**: 입력된 Primary Base Color의 sRGB 값으로부터 OKLCH 색조(Hue) 성분을 추출하고 분석하여 어울리는 그레이 톤을 자동 전환 및 매핑합니다.
  * 💛 **Warm Gray (Hue 45°)**: 색조가 `0° ~ 90°` 또는 `330° ~ 360°` 대역일 때 매칭 (Red, Orange, Yellow 계열)
  * 💙 **Slate (Hue 260°)**: 색조가 `150° ~ 280°` 대역일 때 매칭 (Green, Teal, Blue 계열)
  * ⚪ **True Neutral (Chroma 0)**: 그 외 `90° ~ 150°` 또는 `280° ~ 330°` 대역일 때 매칭
* **수동 오버라이드(Manual Override)**: 오토 페어링이 작동한 상태에서도 드롭다운 선택을 통해 임의로 원하는 그레이 톤을 수동 변경할 수 있습니다.

---

## 🌓 3. 다크 모드 교차 반전 및 불변성(Immutability) 보장

### ① 다크 모드 교차 반전 (Symmetric Inversion)
- 복잡한 명도/채도 감쇄 배율 대신, 라이트 모드 기준 50~950단계 연산 결과물(L, C, H) 배열을 정확히 역순(Reverse)으로 뒤집어 매치합니다.
- 예: 다크 모드의 `50`단계 = 라이트 모드 `950`단계의 값(L 8.0%) / 다크 모드의 `950`단계 = 라이트 모드 `50`단계의 값(L 98.0%)

### ② 팰린드롬 오류 차단 및 불변성 보장
- 원본 데이터(`rawStepsData`)는 항상 라이트 모드 기본형으로 보존하고 다크 모드 렌더링 시에만 독립된 복사 배열(`displayStepsData`)을 생성하여 반전 연산을 매칭하도록 구현했습니다.

---

## ♿ 4. 웹 접근성 (WCAG 2.1) 및 시인성 보정

### ① 완벽한 다크 슬레이트 (#0F172A) 기준 대비비 연산
- 실제 디자인 환경에서 이 무채색들이 배경색으로 쓰이고 그 위에 콘텐츠 텍스트가 배치될 때의 웹 접근성을 정확히 판정하기 위해, 대비율 연산 기준값을 **`#0F172A` (상대 휘도 0.0088)** 및 **`#FFFFFF` (상대 휘도 1.0)**로 철저히 고정하여 WCAG 2.1 표준 등급(PASS/FAIL)을 판정합니다.

### ② UI 글자색 이원화 격리 공학
- 다크 테마 배경의 제너레이터 내에서 카드 위의 대비 판정 등급을 디자이너가 편안하게 읽을 수 있도록, 실제 화면에 그려지는 라벨 글자색은 적당한 가독성을 확보한 보정된 회색 **`oklch(44.6% 0.043 257.281)` (#4B546C)**로 우회하여 렌더링을 처리했습니다.

---

## ⚙️ 5. SSOT 아키텍처 및 Figma Variables 연동

### ① 단일 원천(SSOT, Single Source of Truth) 전역 파이프라인
- 기존 UI 렌더링 함수와 JSON 내보내기 함수가 독자적으로 OKLCH ➡️ sRGB 변환을 중복 수행하면서 발생했던 소수점 float 반올림 불일치 버그(예: 100단계 UI `#D7DBE2` vs JSON `#D6DBE2`)를 완전 박멸했습니다.
- `calculateScaleForMode()`에서 1회만 계산된 11개 칩의 정수형 RGB 기반 `hex` 및 `coords` 세트를 전역 상태(`state.lightScaleData`, `state.darkScaleData`)에 적재한 후, UI 렌더링, CSS 코드 박스, Figma JSON 내보내기/클립보드가 모두 이 전역 상태에서 Read-Only로 읽어오도록 아키텍처를 개편했습니다.

### ② True Neutral 변환 붕괴 방지 (NaN Hue Handling)
- 채도(Chroma)가 0인 True Neutral 모드 선택 시 Hue 값이 `NaN` 또는 `null`로 인식되어 Color.js에서 `#NANNANNAN` 오류로 깨지는 현상을 방지하고자, 연산 엔진에 진입하기 직전 예외 처리를 걸어 Hue를 강제로 `0`으로 자동 전환하여 연산이 수행되도록 처리했습니다.

---

## 🛠️ 6. 주요 기능 목록 (Feature List)

- **11단계(50~950) 무채색 스케일**: Tailwind CSS v4.3 최신 무채색 규격 동기화.
- **오토 페어링 토글**: Primary Base Color 입력에 따른 자동 페어링 톤 제안 및 수동 변경 지원.
- **실시간 슬라이더 연동**: 실시간으로 최대/최소 명도(minL 8.0%)를 시각적으로 변경하며 대비 등급 및 컬러칩 동적 렌더링.
- **테마 미리보기 토글**: 제너레이터 화면 전체의 스케일 표시를 Light / Dark 모드로 전환하여 시각적 확인 기능 제공.
- **원클릭 코드 복사**: 컬러칩 클릭 시 HEX 및 OKLCH 개별 복사 지원.
- **개발자 익스포트 패널**: SSOT 기반 CSS Custom Properties 코드 복사 및 Figma Variables 연동을 위한 Light/Dark JSON 복사 및 다운로드 지원.

---

## 📊 7. 아키텍처 및 기술 스택

- **Structure**: `HTML5`
- **Styling**: `Vanilla CSS3` (CSS Variables, Flexbox, blur backdrop filter)
- **Logic**: `Vanilla Javascript (ES6+)`
- **Color Engine**: `Colorjs.io` (정밀 색상 공간 변환용 글로벌 라이브러리)
