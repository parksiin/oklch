# OKLCH 다차원 컬러 팔레트 제너레이터 스펙 및 기능 설명서

본 문서는 디자이너와 개발자가 함께 협업하여 구축한 **OKLCH 다차원 컬러 팔레트 제너레이터(Multi-dimensional Color Palette Generator)**의 핵심 개념, 동작 메커니즘, 주요 기능 및 기술 스펙을 정리한 종합 명세서입니다.

---

## 🌟 1. 서비스 개요 (Overview)

**OKLCH 다차원 컬러 팔레트 제너레이터**는 현대 웹 표준 색상 공간인 **OKLCH**를 기반으로 작동하는 프리미엄 컬러 스케일 생성 도구입니다. 

기존의 sRGB나 HSL 색상 공간이 가진 "명도와 채도의 왜곡 현상(예: 노란색은 밝고 파란색은 어두운 현상)"을 보완하고, 인간의 실제 시각적 인지 흐름에 일치하도록 정밀하게 설계되었습니다. UI 디자인 시스템에서 가장 중요한 **브랜드 기본 색상(Base Color)을 기준으로 Tailwind CSS v4.3 최신 표준 규격에 발맞춘 상용 레벨의 11단계(50~950) 완벽 대칭 컬러 팔레트를 실시간 수학적 연산을 통해 도출**합니다.

---

## ⚙️ 2. 핵심 물리/수학적 보정 엔진

이 제너레이터가 일반적인 팔레트 생성기와 구별되는 가장 큰 강점은 **3대 다차원 색상 보정 엔진**이 탑재되어 있다는 점입니다.

### ① 하이브리드 명도 가변 스케일링 시스템 (대안 C - Hybrid Variable Lightness Scaling)
* **도입 배경**: 기존의 고정형 명도 오프셋 가산 방식은 기준색(Base Color)의 명도에 관계없이 일괄적으로 명도를 더하거나 빼어, 극단적으로 밝거나 어두운 색 입력 시 양 끝단이 100%나 0%로 압착되어 스케일 단계가 뭉개지는 '명도 압착(Lightness Crunch)' 현상을 야기했습니다. 이는 UI/UX 시스템의 시맨틱 매핑(Semantic Mapping) 뼈대를 무너뜨리는 결함이었습니다.
* **메커니즘 (수학적 보간 공식)**: 
  * 11단계 스케일 확장으로 **500단계가 정중앙 메인 앵커(Base Color Anchor)** 지위를 갖습니다.
  * 500단계에 브랜드 고유 기준색 명도(`baseL`)를 보존하면서, 밝은 한계선(98.0%)과 유채색 Gamut 파괴 방지를 위한 **어두운 한계선(`minChromL = 25.0%`)** 경계선 사이에서 각 단계의 명도 오프셋(`cfg.lOffset`)만큼 비선형 동적 보간(Lerp) 배분합니다.
  * **밝은 대역 (`lOffset > 0`, 50~400단계)**: 단일 지수 수식 폐기 후 시각 인지 튜닝 고정 비율 매핑(`lRatios = { 50: 1.00, 100: 0.92, 200: 0.76, 300: 0.56, 400: 0.28 }`) 적용 ➡️ `targetL = baseL + (98 - baseL) * lRatios[level]` *(※ 400↔500단계 명도 절벽 Cliff 100% 차단 및 완경사 연착륙)*
  * **어두운 대역 (`lOffset < 0`, 600~950단계)**: `targetL = baseL - (baseL - 25) * (Math.abs(cfg.lOffset) / 0.45)` *(※ 유채색 명도 하한선 Floor 25.0% 방어)*
* **다크 모드 스케일 대칭 역상 (Symmetric Inversion)**:
  * 라이트 모드: `50단계` = 가장 밝은 배경(L 98.0%), `950단계` = 가장 어두운 글씨(L 25.0% Floor 방어)
  * **다크 모드**: `50단계` = 어두운 배경(L 25.0% Floor 방어), `500단계` = 다크 메인 앵커(채도 25% 감쇄), `950단계` = 가장 밝은 하이라이트 텍스트(L 98.0%)로 명도 스케일축이 완벽히 대칭 역상되어 단조 증가(Monotonic Rise)합니다.
* **효과**: 노랑(Yellow, L=95%↑)이나 네이비(Navy, L=20%↓) 등 어떤 명도의 브랜드 색상이 입력되더라도 50~100단계(배경) 및 900~950단계(텍스트/하이라이트)의 시맨틱 용도와 WCAG 2.1 웹 접근성 대비율을 안전하게 방어하며 딥 다크 구간의 채도 증발(흙탕물화)을 완벽 차단합니다.

### ② 채도 감쇄 곡선 및 상단 대역 동적 `cScale` Tapering 파이프라인
* **메커니즘**: 명도가 매우 높아지거나(50단계) 매우 낮아질 때(950단계) 채도가 유지되면 색상이 형광빛을 띠거나 시각적으로 찢어지는 현상을 방지합니다.
* **상단 대역 동적 `cScale` Tapering 엔진**: 일괄 비율 적용 시 Yellow/Green 계열의 50~200단계가 무채색(회색/흰색)으로 탈색되는 결함을 막기 위해, Munsell 10앵커 기반 동적 `cScale` 매트릭스(Yellow 2.3, GY 2.1, G 2.0, YR 1.5, R/B 1.0)와 500단계 착륙 수렴 수식(`effectiveScale = 1.0 + (dynamicCScale - 1.0) * factor`)을 이식했습니다.
* **지원 모드**:
  * `Standard`: 50(0.06), 100(0.14), 200(0.27), 300(0.48), 400(0.76), 500(1.00) ➡️ 눈이 편안한 차분한 파스텔 UI 캔버스 생성.
  * `Subtle`: 50(0.04), 100(0.10), 200(0.20), 300(0.40), 400(0.70), 500(1.00) ➡️ 극도로 차분하고 모던한 파스텔 톤 생성.

### ③ 먼셀 10색상환 기반 선형 보간(Lerp) Auto Hue Shifting 엔진
* **메커니즘**: 기존의 3분할 조건문(if/else)이 가질 수밖에 없었던 경계선 불연속성(Hard Edge)을 근본적으로 차단하기 위해, **Munsell 10대 주요 색상축(`R, YR, Y, GY, G, BG, B, PB, P, RP`) 앵커 매트릭스를 기반으로 한 선형 보간(Lerp) 엔진**을 탑재했습니다.
  * **10앵커 매트릭스**: R(25°), YR(45°), Y(100°), GY(130°), G(150°), BG(180°), B(250°), PB(280°), P(310°), RP(350°)
  * **360° Wrap-around 연속 공간(Continuous Space) 연산**: 0°~25° 구간 입력 시 `+360°`를 보정하여 350°~385° 연속 수직선 상에서 선형 보간 비례 연산($t$) 및 방향성 Clamping(`Math.max / Math.min`) 방어를 수행합니다.
  * **RP 330° 종착지 보정 및 순수 델타 반환**: `RP(350°)`의 방어선을 `clamp: 330`으로 보정하여 역클램핑을 차단하고, 연속 공간에서 연산된 무결한 순수 시프트 델타 오프셋(`finalHue - hVal`)을 반환하여 0°/360° 경계선 부근의 렌더링 파손을 100% 차단합니다.

---

## ♿ 3. 웹 접근성 (WCAG 2.1) 및 물리적 격리 시스템

저시력자 및 색약 사용자를 포함한 모든 사용자가 UI 텍스트를 명확하게 인지할 수 있도록 돕는 **가독성 안전장치**입니다.

### ① 정밀 상대 휘도(Relative Luminance) 변환 및 실시간 대비비 연산
* **메커니즘**: 감마 보정이 적용된 sRGB 색상 채널을 선형(Linear) 공식으로 변환하여, 인간의 눈에 실제로 느껴지는 상대적 밝기(Luminance)를 연산합니다. 
* **결과 판정**: 기준이 되는 배경색 대비 명암비(Contrast Ratio)를 실시간 계산하여 등급을 매깁니다.
  * 🟢 **PASS(N) (4.5:1 이상)**: 일반 본문(12~14px)을 포함한 화면 내 모든 텍스트에 안전하게 사용 가능.
  * 🟡 **PASS(L) (3.0:1 이상)**: 큰 제목(18pt↑)이나 아이콘, 라인 요소에만 제한적으로 사용 가능.
  * 🔴 **FAIL (3.0:1 미만)**: 가독성 기준 미달로 텍스트 사용 불가능.

### ② 이원화 격리 공학 (Decoupling)
본 제너레이터의 완성도를 획기적으로 올린 핵심 설계 사양입니다.
* **수학적 연산 기준값 (Back-end)**: 실제 상용 프로덕트(라이트 모드 등)에서 이 컬러칩을 배경으로 쓰고 그 위에 어두운 텍스트를 얹을 때의 적합성을 판정해야 하므로, 대비율 연산용 다크 기준값은 완벽한 다크 슬레이트 **`#0f172a` (상대 휘도 0.0088)**로 고정 계산합니다.
* **화면 렌더링 글자색 (Front-end UI)**: 제너레이터 자체가 다크 테마 배경(`rgba(15, 23, 42, 0.45)`)을 쓰고 있기 때문에 카드 위 글자색이 지나치게 어두우면 사용자가 수치를 읽을 수 없습니다. 따라서 화면 라벨 가독성을 극대화하기 위해 UI 렌더링 글자색은 적당히 밝고 시인성이 확보된 **`oklch(44.6% 0.043 257.281)` (#4B546C)**를 유지합니다.

---

## 🛠️ 4. 주요 제공 기능 (Feature List)

### ① 양방향 실시간 동기화 폼 컨트롤
* **조작 방식**: HEX 텍스트 박스 타이핑, 명도/채도/색상환 개별 슬라이더 조작, 숫자 입력창 타이핑 중 어떤 방식으로 값을 조작하더라도 **모든 입력 필드와 컬러 팔레트가 오차 없이 즉시 동기화**됩니다.

### ② 토스 컬러 시스템 레퍼런스 프리셋 (TDS Presets)
* **기능**: 토스(Toss) 디자인 시스템의 대표 7가지 메인 컬러(`Blue`, `Red`, `Orange`, `Yellow`, `Green`, `Teal`, `Purple`)의 베이스 HEX 코드를 클릭 한 번으로 불러올 수 있는 프리셋 버튼을 탑재하여 벤치마킹 및 빠른 시작을 돕습니다.

### ③ URL 쿼리 파라미터 연동 및 상태 보존 (State Persistence)
* **공유 편의성**: Base Color를 변경하면 브라우저의 URL 주소도 즉시 업데이트됩니다 (예: `?hex=3182F6`). 탭바 전환을 하거나 링크를 복사하여 동료에게 공유해도 사용자가 설정했던 HEX 컬러가 그대로 유지된 채 초기 렌더링됩니다.
* **유연한 예외 처리**: `#FFF`, `#000` 등 3자리 단축 HEX 코드의 입력에도 안전하게 대응할 수 있는 정규식 검증 로직이 내장되어 있습니다.

### ④ 다크 모드 11단계 대칭 역상 및 채도 감쇄 연산 (Dark Mode Scaling)
* **기능**: 헤더의 라이트(Light)/다크(Dark) 탭 전환 시, 단순 명도 뒤집기가 아닌 다크 모드에 최적화된 **25% 채도 감쇄 및 50(어두운 배경 8%) ➡️ 500(다크 앵커) ➡️ 950(밝은 글씨 98%) 대칭 역상 보정 알고리즘**이 적용되어 눈이 편안하고 가독성 높은 다크 모드 팔레트 세트를 별도 연산하여 렌더링합니다.

### ⑤ 코드 내보내기 및 데이터 다운로드 (Figma Tokens SSOT)
* **클립보드 복사**: 단계별 컬러칩에서 마우스 클릭 한 번으로 `HEX` 코드 및 `OKLCH` 함수 문자열을 신속하게 클립보드에 복사할 수 있습니다.
* **개발자 패널**: 우측 하단에서 완성된 11단계 팔레트 전체의 **CSS 변수 코드** 및 **JSON 데이터**를 즉시 확인하고 클라이언트 사이드에서 파일로 직접 다운로드(`downloadFigmaJSON`)할 수 있는 내보내기 창을 제공합니다.
* **SSOT 무결성**: 다운로드되는 Figma JSON 파일명과 데이터는 선 연산된 전역 상태 메모리(`state.lightPalette`, `state.darkPalette`)의 500단계 HEX(`hex500`)를 100% 참조하여 오차가 발생하지 않습니다.

### ⑥ 하단 독립형 OKLCH 그라데이션 테스터 (Gradient Tester)
* **경로 최적화**: 시작(Start)과 끝(End) HEX 코드를 입력하면, OKLCH 구형 색상각 상에서 가장 거리가 짧은 **최단 경로(Shortest Path) Hue 보간 알고리즘**을 통해 어색한 무지개 현상 없는 가장 부드럽고 매끄러운 그라데이션을 시각화합니다.
* **미드포인트 추출**: 그라데이션의 한가운데(50% Midpoint) 색상을 자동으로 연산하여 단일 HEX 코드로 즉시 복사할 수 있는 기능을 제공합니다.
* **CSS 코드 복사**: 일반 브라우저용 `sRGB` 그라데이션 CSS 및 최신 브라우저용 `linear-gradient(in oklch, ...)` 코드를 원클릭으로 추출할 수 있습니다.

---

## 📊 5. 시스템 아키텍처 및 기술 스택

* **Markup/Structure**: `HTML5` (Semantic Elements 활용)
* **Styling**: `Vanilla CSS3` (CSS Custom Variables, CSS Grid/Flexbox, Relative Color Syntax)
* **Logic/Math**: `Vanilla Javascript` (ES6+, Colorjs.io 라이브러리 연계)
* **정적 라이브러리**: [Colorjs.io](https://colorjs.io/) (정밀 색상 공간 변환 및 좌표 산출용)

---

## 📝 6. 아키텍처 개편 및 변경 이력 (Revision History)

본 도구의 지속 가능성과 안정성을 확보하기 위해 발생했던 주요 결함과 아키텍처 개편 이력을 기록합니다.

### 1) [아키텍처 개편] 메인 제너레이터 명도 압착 결함 해결 (대안 C)
* **기존 방식의 문제점 (결함)**: 500단계에 기준색을 고정하고 타 단계에 단순 고정 오프셋(예: L + 0.35, L - 0.40)을 적용할 경우, 노란색(L=95%↑) 입력 시 50~400단계가 전부 화이트아웃(White out)되고, 어두운 네이비(L=20%↓) 입력 시 600~900단계가 전부 블랙아웃(Black out)되는 '명도 압착(Lightness Crunch)' 현상이 발생했습니다.
* **해결 (대안 C)**: 500단계 명도는 100% 보존하면서, 양 끝단의 한계선(98%, 15%)을 기준으로 가용 범위 내에서 명도를 비선형 동적 보간(Lerp) 스케일링하는 '하이브리드 명도 가변 스케일링(대안 C)'을 도입하여 해결했습니다.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 2) [핫픽스] Figma Variables 임포트 호환용 `$value.hex` 속성 복구
* **변경 사유**: DTCG 표준에 맞추기 위해 제거했던 `$value` 객체 하위의 `"hex": "#..."` 속성이 피그마 Variables 임포트 플러그인의 파싱 및 하위 호환성에 필수적임을 인지하여, `Color.js` 엔진을 통해 동적 sRGB hex를 추출하여 JSON 구조 내에 긴급 복구했습니다.

### 3) [핫픽스] CSS Relative Color Syntax 파싱 실패 결함 해결
* **변경 사유**: 명도 변수 도입 시 CSS의 `oklch()` 색상 함수 내에 기준색 분해 구문인 `from var(--base-color)`가 누락되어 브라우저가 색상 파싱에 실패하고 검은색(`#000000`)으로 렌더링되던 심각한 렌더링 결함을 해결하기 위해 `from var(--base-color)` 구문을 전면 복구했습니다.

### 4) [아키텍처 개편] Double Calculation 제거 및 JS 단일 원천(SSOT) 아키텍처 구축
* **변경 사유 (결함)**: 브라우저의 CSS oklch 렌더링 파이프라인과 자바스크립트 `Color.js` 연산 결과 간의 미세한 파싱 변환 차이로 인해, UI 화면에 표시되는 HEX 값과 Figma 임포트용 JSON 데이터 내부의 HEX 값 사이에 1단위 미세 오차가 발생하는 현상(Double Calculation 결함)이 발견되었습니다.
* **해결 사양**:
  * **JS 단일 원천화(SSOT)**: 렌더링 시 브라우저 엔진에 변환을 위임하지 않고, JS 메모리 단에서 `Color.js` 및 Canvas 2D를 사용해 Light/Dark 각각의 HEX를 선 계산하여 전역 상태(`state.lightPalette`, `state.darkPalette`)에 먼저 적재하도록 구조를 개편했습니다.
  * **직접 인라인 주입**: UI 렌더링 함수(`syncAndRender`)에서 이 선 계산된 HEX 값을 각 컬러칩 스와치에 직접 인라인 배경색(`style.backgroundColor`)으로 주입하여 화면상의 HEX 값과 연산 결과를 100% 일치시켰습니다.
  * **Components 역산 및 5자리 정밀도 상향**: Figma JSON을 빌드할 때도 사전 적재된 HEX 값을 기준으로 components sRGB 비율을 역산하여 내보냅니다.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 5) [핫픽스 및 아키텍처 보완] 다크 400단계 튐 결함 복구, Base Color 뷰 격리 및 Chroma Easing 상태 보존
* **변경 사유 (결함)**: 다크 모드 400단계 Bypass 로직으로 인한 고채도 튐 현상, 탭 전환 시 baseHex 누적 왜곡 무한 루프, 프리셋 클릭 시 Chroma Easing 리셋 현상 발생.
* **해결 사양**: 다크 400단계 Bypass 삭제 및 채도 25% 감쇄 정상 적용, 오리지널 `state.baseHex` 소스 일원화로 무한 루프 원천 차단, Easing 모드 선택자 격리 및 `&easing=` URL/LocalStorage 상태 보존 이식.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 6) [아키텍처 대개편] Tailwind v4.3 11단계(50~950) 확장 & 500 앵커 대칭 역상 엔진 이식
* **개편 배경**: 최신 Tailwind CSS v4.3 컬러 표준에 부합하는 11단계 스케일 시스템 확장 및 무채색/유채색 제너레이터 간 연산 규격 통일.
* **핵심 변경 사양**:
  1. **11단계(50~950) 대칭 구조 확장**: 스케일을 10단계(50~900)에서 11단계(50~950)로 확장하고, 500단계를 상하 5개씩 대칭을 이루는 정중앙 메인 앵커(Anchor)로 설정.
  2. **최소 명도 `minL` 8.0% 하향**: 950단계 신설에 따라 최소 명도 타겟을 `8.0%`로 확대하고, 어두운 대역 연산 분모를 `0.45`로 확장 (`targetL = baseL - (baseL - 8) * (Math.abs(lOffset) / 0.45)`).
  3. **다크 모드 메인 앵커 500단계 이관 및 채도 25% 감쇄 연동**: 다크 모드 기준 칩을 400단계에서 500단계로 이관하고, 500단계 칩이 다크 25% 채도 감쇄(`* 0.75`) 파이프라인을 정상 타도록 보정. (라이트 모드 500단계만 원본 baseHex Bypass 유지)
  4. **다크 모드 스케일축 대칭 역상(Symmetric Inversion) 복구**: 다크 모드 명도 오프셋 부호를 보정하여 `50단계` = 어두운 배경(L 8.0%) ➡️ `500단계` = 다크 앵커(L = baseL) ➡️ `950단계` = 밝은 하이라이트 텍스트(L 98.0%)로 단조 증가하는 다크 스케일축 확립.
  5. **Figma JSON 다운로드 SSOT 일원화**: `downloadFigmaJSON(mode)` 호출 시 JS 전역 상태 메모리의 선 계산된 500단계 HEX(`hex500`)를 직접 참조하여 100% 무결한 토큰 파일이 내려받아지도록 다운로드 엔진 시정.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 7) [아키텍처 대개편] 먼셀 10색상환 기반 선형 보간(Lerp) Auto Hue Shifting 엔진 이식
* **개편 배경**: 기존 3분할 조건문(if/else)의 구간 경계선 불연속성(Hard Edge) 제거 및 0.1° 단위의 미세 조작 시 실크처럼 매끄러운 자연스러운 Hue Shift 곡선 형성.
* **핵심 변경 사양**:
  1. **Munsell 10대 주요 색상축 앵커 정의**: R(25°), YR(45°), Y(100°), GY(130°), G(150°), BG(180°), B(250°), PB(280°), P(310°), RP(350°) 매트릭스 주입.
  2. **360° Wrap-around 연속 공간(Continuous Space) Clamping 방어**: 중간 모듈로(% 360) 연산을 금지하고 1차원 수직선 상에서 방어를 끝낸 뒤 순수 델타 오프셋(`finalHue - hVal`)을 반환하여 0°/360° 경계선 폭주 및 Modulo Trap 결함을 원천 박멸.
  3. **RP 330° 종착지 정밀 보정**: `RP(350°)`의 역클램핑 튕김을 막기 위해 `clamp: 330`을 수록하여 330° 지점 깔끔 안착 보장.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 8) [아키텍처 대개편] 유채색 상단 대역(50~400) 명도 Ease-out 0.55 & 먼셀 10앵커 동적 cScale Tapering 파이프라인 이식
* **개편 배경**: 50~200단계 캔버스 배경 렌더링 시 발생하던 지나친 채도 튐(원색/형광 현상) 및 Yellow/Green 계열의 회색/흰색 무채색 탈색 버그 100% 시정.
* **핵심 변경 사양**:
  1. **명도 Ease-out 0.55 S-Curve 이식**: 50단계(98.0%) ➡️ 100단계(96.8%) ➡️ 200단계(94.3%)까지 명도를 하얗고 깨끗하게 오랫동안 유지(Ease-out)시킨 후 베이스 명도로 연착륙.
  2. **Tailwind 실측 cRatio 파스텔 스케일 주입**: 50단계 `0.06`, 100단계 `0.14`, 200단계 `0.27`, 300단계 `0.48`, 400단계 `0.76`, 500단계 `1.00` 적용.
  3. **먼셀 10앵커 동적 cScale Tapering 엔진 이식**: Yellow(`cScale 2.3`), GY(`2.1`), G(`2.0`), YR(`1.5`), R/B(`1.0`) 매트릭스 주입 및 500단계 착륙 수렴 수식(`effectiveScale = 1.0 + (dynamicCScale - 1.0) * factor`)으로 300~500단계 Chroma Flatline(채도 일직선 찌그러짐) 버그 완전 차단.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 9) [아키텍처 대개편] 유채색 상단 대역(50~400) 명도 고정 비율 매핑(lRatios) 아키텍처 이식
* **개편 배경**: 단일 지수 수식(`Math.pow(factor, 0.55)`) 적용 시 400단계 ↔ 500단계 사이에서 발생하던 급격한 시각적 명도 절벽(Cliff) 현상을 100% 시정하고 뉴트럴 제너레이터의 검증된 고정 비율 매핑 방식 주입.
* **핵심 변경 사양**:
  1. **lRatios 비율 매핑 테이블 신설**: `{ 50: 1.00, 100: 0.92, 200: 0.76, 300: 0.56, 400: 0.28 }` 주입으로 400↔500단계 명도 변화 폭을 기존 15.0%p 절벽에서 9.6%p의 완경사로 개선.
  2. **채도 엔진 완벽 격리**: 명도 연산 수식이 비율 매핑으로 교체되더라도 기존 '먼셀 10앵커 동적 cScale Tapering' 채도 엔진용 `factor` 변수를 독립 보완하여 렌더링 무결성 보존.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 10) [신규 기능] 피그마 다중 팔레트 비교 검증용 '멀티 모드(Multi-Mode) JSON 콜렉터' 파이프라인 이식
* **기능 개요**: 여러 개의 서로 다른 브랜드 컬러 팔레트(예: Red, Yellow, Blue 등)를 차례대로 대기열에 담아 피그마 Variables에 한 번에 임포트하여 나란히 비교·검증할 수 있는 통합 내보내기 엔진.
* **핵심 변경 사양**:
  1. **Glassmorphism 콜렉터 UI 패널 신설**: `[대기열에 현재 팔레트 담기]`, `[대기열 비우기]`, `[통합 JSON 다운로드 (N개)]` 버튼 및 실시간 `N개 대기 중` 뱃지 주입.
  2. **피그마 Native Variables Multi-Mode Columns 스키마 지원**: `$value` 객체 내 모드 식별자 키(`Red_EF4444`, `Blue_3182F6` 등)와 색상값을 주입하여 피그마에서 클릭 한 번으로 모드(Mode Switch) 전환 비교 검증 가능.
  3. **Tokens Studio Multi-Set 독립 스키마 동시 포괄**: `sets` 객체 병합으로 Tokens Studio 및 Native Figma Variables 양쪽 모두 100% 호환.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 11) [핫픽스] 피그마 Native Variables 규격(1 File = 1 Mode) 맞춤 및 개별 JSON 다중 순차 다운로드 파이프라인 시정
* **개편 배경**: 피그마 Native Variables 임포트 플러그인의 물리적 표준 규격인 "1개 JSON 파일 = 1개 Mode (`$extensions.com.figma.modeName` 매핑)" 규격에 맞추어 내보내기 엔진 개편.
* **핵심 변경 사양**:
  1. **1 File = 1 Mode 독립 JSON 스키마 적용**: 대기열 각 스냅샷마다 `$value: "#HEX"` 단일 매핑 및 `$extensions.com.figma.modeName: "Red (#EF4444)"`가 명시된 독립 객체 생성.
  2. **브라우저 팝업 차단 방지 순차 다중 다운로드 엔진 (`downloadNext`)**: 250ms 딜레이를 주어 대기열에 담긴 $N$개의 모드 파일들을 연속 다운로드하여 피그마에서 드래그 앤 드롭으로 나란히 모드 컬럼을 형성하도록 보정.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 12) [아키텍처 대개편] 브라우저 보안 차단 우회를 위한 JSZip 기반 단일 ZIP 압축 다운로드 (`Multi-Palettes-N-Modes.zip`) 파이프라인 이식
* **개편 배경**: Chrome/Brave/Edge/Safari 등 브라우저 보안 정책(Automatic Multiple Downloads Security Policy)으로 인해 JS 다중 다운로드 시 2번째 파일부터 자동 차단되던 문제 100% 해결.
* **핵심 변경 사양**:
  1. **JSZip CDN 임포트 및 인메모리 번들링 파이프라인 구축**: 대기열에 담긴 $N$개의 1 File = 1 Mode 규격 JSON 파일들을 `JSZip` 인메모리 상에서 한 번에 묶어 `Multi-Palettes-N-Modes.zip` 단일 파일로 생성 후 즉시 다운로드.
  2. **피그마 호환성 무결성**: 압축 해제 후 폴더 안의 10개 JSON 파일을 피그마 Variables 임포트 플러그인에 한꺼번에 드래그 앤 드롭하면 피그마에서 10개의 모드(Columns)가 나란히 자동 생성.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 13) [핫픽스] 피그마 Native Variables 파서용 `$value` 확장 객체 스키마 복구 및 RGB(0~1) 정밀 역산 이식
* **개편 배경**: 멀티 모드 JSON 파싱 시 피그마 Variables 임포트 플러그인이 단순 HEX 문자열 방식을 무시하던 파싱 결함 100% 시정.
* **핵심 변경 사양**:
  1. **`hexToRgbComponents(hex)` 정밀 역산 헬퍼 추가**: HEX 코드를 기반으로 R, G, B 비율(0.0~1.0) 배열을 정밀 계산하여 주입.
  2. **피그마 Native `$value` 확장 객체 주입**: `$value: { colorSpace: "srgb", components: [...], alpha: 1, hex: "#HEX" }` 스키마로 복구하여 피그마 플러그인 파싱 성공률 100% 보장.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 14) [핫픽스] `dark.html` 자바스크립트 런타임 크래시 픽스 및 다크모드 500단계 25% 채도 감쇄 스펙 보존
* **개편 배경**: `dark.html` 초기 렌더링 시 미선언 헬퍼 함수(`getDynamicChromaScale`) 참조 에러로 자바스크립트 실행이 중단되던 런타임 크래시 원천 시정.
* **핵심 변경 사양**:
  1. **`getDynamicChromaScale(h)` 및 먼셀 10앵커 `cScale` 이식**: 헬퍼 함수를 수록하여 스크린샷에 보이던 `-` 텍스트 멈춤 현상을 100% 해제하고 11개 스와치 및 WCAG 명암비 수치 렌더링을 완전히 복구.
  2. **다크 모드 500단계 25% 채도 감쇄(`* 0.75`) 규격 보존**: 공식 스펙 명세서 6-3번 항목 규격에 의거하여, 다크모드의 500단계 칩은 어두운 배경 위 캔버스 눈부심(Neon 현상) 방지를 위한 채도 25% 감쇄 연산을 100% 그대로 유효하게 유지.
* **적용 파일**: `dark.html`

### 15) [핫픽스] `dark.html` downloadMultiJson 동적 그룹명 루트 키 규격 통일 및 950 토큰 파싱 정상화
* **개편 배경**: 다크모드 제너레이터(`dark.html`) 멀티 모드 내보내기 시, 개별 다운로드(`buildFigmaJSONSchema`)와 루트 스키마 규격이 달라 피그마 Variables 플러그인에서 950 토큰이 누락되던 결함 원천 시정.
* **핵심 변경 사양**:
  1. **최상위 루트 키 동적 그룹명(`"adaptive" + groupName`) 통일**: 기존 고정 문자열 `"color"`를 폐기하고 개별 다운로드와 100% 동일한 `adaptiveBlue`, `adaptiveRed` 형태의 동적 그룹 키 규격 주입.
  2. **피그마 11단계 토큰 100% 정상 인식 보완**: 스키마 규격을 1:1로 일치시켜 50부터 950까지 11개 토큰 전체가 단 하나도 누락되지 않고 피그마에서 정밀 파싱되도록 보정.
* **적용 파일**: `dark.html`

### 16) [핫픽스] `hexToRgbComponents` 내 3자리 단축 HEX 코드 파싱 방어 및 components null 오염 차단
* **개편 배경**: 3자리 단축 헥스코드(예: `#FEF`, `#FFF`) 생성 시 `hexToRgbComponents`가 Blue 채널을 파싱하지 못해 `NaN` $\rightarrow$ `null`로 변환되어 피그마에서 950 토큰이 Drop 되던 버그 원천 시정.
* **핵심 변경 사양**:
  1. **3자리 HEX 6자리 자동 확장 로직 추가**: `cleanHex.length === 3`일 경우 `char + char` 연산을 거쳐 `#FEF` $\rightarrow$ `#FFEEFF`로 정규화하는 방어 코드 적용.
  2. **피그마 950 토큰 Drop 무결 해결**: components 배열 내 `null` 생성을 원천 차단하여 피그마에서 950 토큰이 100% 정상 파싱되도록 보정.
* **적용 파일**: `index.html`, `dark.html` 2종 전체.

### 17) [핫픽스] `dark.html` downloadMultiJson 최상위 변수 루트 키 `"color"` 통일 및 피그마 다중 모드 병합 정상화
* **개편 배경**: 다크모드(`dark.html`) 멀티 모드 ZIP 내보내기 시, 개별 파일들의 최상위 변수 루트 키가 파일마다 다르게 분리되어 피그마 Variables 임포터에서 N개 다중 모드(Mode Columns) 병합 임포트가 차단되던 결함 원천 시정.
* **핵심 변경 사양**:
  1. **최상위 변수 루트 키 `"color"` 통일**: `dark.html` 내 `downloadMultiJson` 최상위 루트 키를 `index.html`과 100% 동일하게 `"color"`로 통일.
  2. **피그마 N개 파일 다중 모드 병합 임포트 정상화**: 스키마 규격을 라이트모드와 1:1로 맞춰 내려받은 N개 다크모드 JSON 파일들이 피그마에서 단일 컬렉션 하위의 `Red (#EF4444)`, `Blue (#3182F6)` 다중 모드 컬럼으로 나란히 한 번에 100% 정상 임포트되도록 보정.
* **적용 파일**: `dark.html`











