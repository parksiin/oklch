# OKLCH 다차원 컬러 팔레트 제너레이터 스펙 및 기능 설명서

본 문서는 디자이너와 개발자가 함께 협업하여 구축한 **OKLCH 다차원 컬러 팔레트 제너레이터(Multi-dimensional Color Palette Generator)**의 핵심 개념, 동작 메커니즘, 주요 기능 및 기술 스펙을 정리한 종합 명세서입니다.

---

## 🌟 1. 서비스 개요 (Overview)

**OKLCH 다차원 컬러 팔레트 제너레이터**는 현대 웹 표준 색상 공간인 **OKLCH**를 기반으로 작동하는 프리미엄 컬러 스케일 생성 도구입니다. 

기존의 sRGB나 HSL 색상 공간이 가진 "명도와 채도의 왜곡 현상(예: 노란색은 밝고 파란색은 어두운 현상)"을 보완하고, 인간의 실제 시각적 인지 흐름에 일치하도록 정밀하게 설계되었습니다. UI 디자인 시스템에서 가장 중요한 **브랜드 기본 색상(Base Color)을 기준으로 상용 레벨의 10단계(50~900) 컬러 팔레트를 실시간으로 수학적 연산을 통해 도출**합니다.

---

## ⚙️ 2. 핵심 물리/수학적 보정 엔진

이 제너레이터가 일반적인 팔레트 생성기와 구별되는 가장 큰 강점은 **3대 다차원 색상 보정 엔진**이 탑재되어 있다는 점입니다.

### ① 하이브리드 명도 가변 스케일링 시스템 (대안 C - Hybrid Variable Lightness Scaling)
* **도입 배경**: 기존의 고정형 명도 오프셋 가산 방식은 기준색(Base Color)의 명도에 관계없이 일괄적으로 명도를 더하거나 빼어, 극단적으로 밝거나 어두운 색 입력 시 양 끝단이 100%나 0%로 압착되어 스케일 단계가 뭉개지는 '명도 압착(Lightness Crunch)' 현상을 야기했습니다. 이는 UI/UX 시스템의 시맨틱 매핑(Semantic Mapping) 뼈대를 무너뜨리는 결함이었습니다.
* **메커니즘 (수학적 보간 공식)**: 500단계(다크 모드는 400단계)에 브랜드의 고유 기준색 명도(`baseL`)를 100% 보존하며, 밝은 영역(최대 한계 L: 98%)과 어두운 영역(최소 한계 L: 15%) 경계선 사이에서 각 단계의 원래 명도 비율(`cfg.lOffset`)만큼 비선형 동적 보간(Lerp) 배분합니다.
  - **밝은 대역 (50~450)**: `targetL = baseL + (98 - baseL) * (cfg.lOffset / 0.35)`
  - **어두운 대역 (550~900)**: `targetL = baseL - (baseL - 15) * (Math.abs(cfg.lOffset) / 0.40)`
  *(※ 다크 모드는 기준색이 400단계이므로 400단계 미만은 어두운 대역, 400단계 초과는 밝은 대역 공식을 적용합니다.)*
* **효과**: 노랑(Yellow, L=95%↑)이나 네이비(Navy, L=20%↓) 등 어떤 명도의 브랜드 색상이 입력되더라도 50~100단계(배경) 및 800~900단계(텍스트)의 시맨틱 용도와 WCAG 2.1 웹 접근성 대비율을 안전하게 방어합니다.

### ② 채도 감쇄 곡선 (Chroma Easing)
* **메커니즘**: 명도가 매우 높아지거나(50단계) 매우 낮아질 때(900단계) 채도가 유지되면 색상이 형광빛을 띠거나 시각적으로 찢어지는 현상이 발생합니다. 이를 방지하기 위해 단계별로 감쇄 비율을 적용합니다.
* **지원 모드**:
  * `Standard`: 균형 잡힌 선명함을 유지하면서 양 끝단의 형광빛을 방지하는 표준 곡선.
  * `Subtle`: 파스텔 톤에 가깝게 차분하고 고급스러운 채도 감소를 보여주는 부드러운 곡선.

### ③ 지능형 자동 색상 이동 (Auto Hue Shifting)
* **메커니즘**: 색상이 어두워지거나 밝아질 때 인간의 눈에는 탁하게 느껴지는 구간이 존재합니다. 본 엔진은 입력된 색조(Hue) 대역을 실시간으로 감지하여 어두운 단계로 갈수록 자연스럽고 풍부한 톤으로 색상 각도를 미세하게 꺾어줍니다.
  * **레드/주황/노랑 계열**: 어두워질수록 따뜻한 붉은 톤으로 보정 (`-16°` ~ `+12°`)
  * **초록/청록 계열**: 어두워질수록 깊이감 있는 딥한 청록(Teal)으로 보정 (`-6°`)
  * **블루/네이비 계열**: 어두워질수록 깊이 있는 네이비/바이올렛 방향으로 보정 (`+14°`)

---

## ♿ 3. 웹 접근성 (WCAG 2.1) 및 물리적 격리 시스템

저시력자 및 색약 사용자를 포함한 모든 사용자가 UI 텍스트를 명확하게 인지할 수 있도록 돕는 **가독성 안전장치**입니다.

### ① 정밀 상대 휘도(Relative Luminance) 변환 및 실시간 대비비 연산
* **메커니즘**: 감마 보정이 적용된 sRGB 색상 채널을 선형(Linear) 공식으로 변환하여, 인간의 눈에 실제로 느껴지는 상대적 밝기(Luminance)를 연산합니다. 
* **결과 판정**: 기준이 되는 배경색 대비 명암비(Contrast Ratio)를 실시간 계산하여 등급을 매깁니다.
  * 🟢 **PASS(N) (4.5:1 이상)**: 일반 본문(12~14px)을 포함한 화면 내 모든 텍스트에 안전하게 사용 가능.
  * 🟡 **PASS(L) (3.0:1 이상)**: 큰 제목(18pt↑)이나 아이콘, 라인 요소에만 제한적으로 사용 가능.
  * 🔴 **FAIL (3.0:1 미만)**: 가독성 기준 미달로 텍스트 사용 불가능.

### ② 이원화 격리 공학 (Decoupling)
본 제너레이터의 완성도를 획기적으로 올린 핵심 설계 사양입니다.
* **수학적 연산 기준값 (Back-end)**: 실제 상용 프로덕트(라이트 모드 등)에서 이 컬러칩을 배경으로 쓰고 그 위에 어두운 텍스트를 얹을 때의 적합성을 판정해야 하므로, 대비율 연산용 다크 기준값은 완벽한 다크 슬레이트 **`#0f172a` (상대 휘도 0.0088)**로 고정 계산합니다.
* **화면 렌더링 글자색 (Front-end UI)**: 제너레이터 자체가 다크 테마 배경(`rgba(15, 23, 42, 0.45)`)을 쓰고 있기 때문에 카드 위 글자색이 지나치게 어두우면 사용자가 수치를 읽을 수 없습니다. 따라서 화면 라벨 가독성을 극대화하기 위해 UI 렌더링 글자색은 적당히 밝고 시인성이 확보된 **`oklch(44.6% 0.043 257.281)` (#4B546C)**를 유지합니다.

---

## 🛠️ 4. 주요 제공 기능 (Feature List)

### ① 양방향 실시간 동기화 폼 컨트롤
* **조작 방식**: HEX 텍스트 박스 타이핑, 명도/채도/색상환 개별 슬라이더 조작, 숫자 입력창 타이핑 중 어떤 방식으로 값을 조작하더라도 **모든 입력 필드와 컬러 팔레트가 오차 없이 즉시 동기화**됩니다.

### ② 토스 컬러 시스템 레퍼런스 프리셋 (TDS Presets)
* **기능**: 토스(Toss) 디자인 시스템의 대표 7가지 메인 컬러(`Blue`, `Red`, `Orange`, `Yellow`, `Green`, `Teal`, `Purple`)의 베이스 HEX 코드를 클릭 한 번으로 불러올 수 있는 프리셋 버튼을 탑재하여 벤치마킹 및 빠른 시작을 돕습니다.

### ③ URL 쿼리 파라미터 연동 및 상태 보존 (State Persistence)
* **공유 편의성**: Base Color를 변경하면 브라우저의 URL 주소도 즉시 업데이트됩니다 (예: `?hex=3182F6`). 탭바 전환을 하거나 링크를 복사하여 동료에게 공유해도 사용자가 설정했던 HEX 컬러가 그대로 유지된 채 초기 렌더링됩니다.
* **유연한 예외 처리**: `#FFF`, `#000` 등 3자리 단축 HEX 코드의 입력에도 안전하게 대응할 수 있는 정규식 검증 로직이 내장되어 있습니다.

### ④ 다크 모드 스케일 자동 반전 (Dark Mode Scaling)
* **기능**: 헤더의 라이트(Light)/다크(Dark) 탭 전환 시, 단순 명도 뒤집기가 아닌 다크 모드에 최적화된 **25% 채도 감쇄 및 명도 스케일 보정 알고리즘**이 적용되어 눈이 편안하고 가독성 높은 다크 모드 팔레트 세트를 별도 연산하여 렌더링합니다.

### ⑤ 코드 내보내기 및 데이터 다운로드
* **클립보드 복사**: 단계별 컬러칩에서 마우스 클릭 한 번으로 `HEX` 코드 및 `OKLCH` 함수 문자열을 신속하게 클립보드에 복사할 수 있습니다.
* **개발자 패널**: 우측 하단에서 완성된 10단계 팔레트 전체의 **CSS 변수 코드** 및 **JSON 데이터**를 즉시 확인하고 단일 파일로 다운로드할 수 있는 내보내기 창을 제공합니다.

### ⑥ 하단 독립형 OKLCH 그라데이션 테스터 (Gradient Tester)
* **경로 최적화**: 시작(Start)과 끝(End) HEX 코드를 입력하면, OKLCH 구형 색상각 상에서 가장 거리가 짧은 **최단 경로(Shortest Path) Hue 보간 알고리즘**을 통해 어색한 무지개 현상 없는 가장 부드럽고 매끄러운 그라데이션을 시각화합니다.
* **미드포인트 추출**: 그라데이션의 한가운데(50% Midpoint) 색상을 자동으로 연산하여 단일 HEX 코드로 즉시 복사할 수 있는 기능을 제공합니다.
* **CSS 코드 복사**: 일반 브라우저용 `sRGB` 그라데이션 CSS 및 최신 브라우저용 `linear-gradient(in oklch, ...)` 코드를 원클릭으로 추출할 수 있습니다.

---

## 📊 5. 시스템 아키텍처 및 기술 스택

* **Markup/Structure**: `HTML5` (Semantic Elements 활용)
* **Styling**: `Vanilla CSS3` (CSS Custom Variables, CSS Grid/Flexbox, Relative Color Syntax)
* **Logic/Math**: `Vanilla Javascript` (ES6+, Colorjs.io 라이브러리 연계)
* **정적 라이브러리**: [Colorjs.io](https://colorjs.io/) (정밀 색상 공간 변환 및 좌표 산출용)

---

## 📝 6. 아키텍처 개편 및 변경 이력 (Revision History)

본 도구의 지속 가능성과 안정성을 확보하기 위해 발생했던 주요 결함과 아키텍처 개편 이력을 기록합니다.

### 1) [아키텍처 개편] 메인 제너레이터 명도 압착 결함 해결 (대안 C)
* **기존 방식의 문제점 (결함)**: 500단계에 기준색을 고정하고 타 단계에 단순 고정 오프셋(예: L + 0.35, L - 0.40)을 적용할 경우, 노란색(L=95%↑) 입력 시 50~400단계가 전부 화이트아웃(White out)되고, 어두운 네이비(L=20%↓) 입력 시 600~900단계가 전부 블랙아웃(Black out)되는 '명도 압착(Lightness Crunch)' 현상이 발생했습니다. 이는 디자인 시스템에서 고정한 '시맨틱 매핑 매트릭스'를 붕괴시키는 비즈니스 리스크였습니다.
* **해결 (대안 C)**: 500단계 명도는 100% 보존하면서, 양 끝단의 한계선(98%, 15%)을 기준으로 가용 범위 내에서 명도를 비선형 동적 보간(Lerp) 스케일링하는 '하이브리드 명도 가변 스케일링(대안 C)'을 도입하여 해결했습니다.
* **적용 파일**: `index.html`, `light.html`, `dark.html` 3종 전체의 UI, CSS, Figma JSON 추출 로직 동기화.

### 2) [핫픽스] Figma Variables 임포트 호환용 `$value.hex` 속성 복구
* **변경 사유**: DTCG 표준에 맞추기 위해 제거했던 `$value` 객체 하위의 `"hex": "#..."` 속성이 피그마 Variables 임포트 플러그인의 파싱 및 하위 호환성에 필수적임을 인지하여, `Color.js` 엔진을 통해 동적 sRGB hex를 추출하여 JSON 구조 내에 긴급 복구했습니다.

### 3) [핫픽스] CSS Relative Color Syntax 파싱 실패 결함 해결
* **변경 사유**: 명도 변수 도입 시 CSS의 `oklch()` 색상 함수 내에 기준색 분해 구문인 `from var(--base-color)`가 누락되어 브라우저가 색상 파싱에 실패하고 검은색(`#000000`)으로 렌더링되던 심각한 렌더링 결함을 해결하기 위해 `from var(--base-color)` 구문을 전면 복구했습니다.

### 4) [아키텍처 개편] Double Calculation 제거 및 JS 단일 원천(SSOT) 아키텍처 구축
* **변경 사유 (결함)**: 브라우저의 CSS oklch 렌더링 파이프라인(Gamut 클리핑, 감마 보정 등)과 자바스크립트 `Color.js` 연산 결과 간의 미세한 파싱 변환 차이로 인해, UI 화면에 표시되는 HEX 값과 Figma 임포트용 JSON 데이터 내부의 HEX 값 사이에 1단위 미세 오차가 발생하는 현상(Double Calculation 결함)이 발견되었습니다.
* **해결 사양**:
  * **JS 단일 원천화(SSOT)**: 렌더링 시 브라우저 엔진에 변환을 위임하지 않고, JS 메모리 단에서 `Color.js` 및 Canvas 2D를 사용해 Light/Dark 각각의 10단계 HEX를 선 계산하여 전역 상태(`state.lightPalette`, `state.darkPalette`)에 먼저 적재하도록 구조를 개편했습니다.
  * **직접 인라인 주입**: UI 렌더링 함수(`syncAndRender`)에서 이 선 계산된 HEX 값을 각 컬러칩 스와치에 직접 인라인 배경색(`style.backgroundColor`)으로 주입하여 화면상의 HEX 값과 연산 결과를 100% 일치시켰습니다.
  * **Components 역산 및 5자리 정밀도 상향**: Figma JSON을 빌드할 때도 사전 적재된 HEX 값을 기준으로 components sRGB 비율을 역산하여 내보냅니다. 이때 피그마 렌더링 엔진의 소수점 반올림 오차 재발 방지를 위해 components 정밀도를 기존 3자리(`.toFixed(3)`)에서 5자리(`.toFixed(5)`)로 상향 적용하여 DTCG 규격의 안전성을 보장하였습니다.
  * **앵커 레벨 절대 보존**: Base Color에 해당하는 라이트 500단계, 다크 400단계에 대해서는 사용자가 입력한 HEX 원본을 연산 없이 그대로 보존(Bypass)하여 부동 소수점 오차가 발생하지 않도록 강제했습니다.
* **적용 파일**: `index.html`, `light.html`, `dark.html` 3종 전체의 UI 및 JSON 데이터 파이프라인 동기화.

### 5) [핫픽스 및 아키텍처 보완] 다크 400단계 튐 결함 복구, Base Color 뷰 격리 및 Chroma Easing 상태 보존
* **변경 사유 (결함)**:
  * **다크 400단계 튐 현상**: 다크 모드는 눈부심 방지를 위해 채도를 25% 일괄 감쇄하는데, 다크 모드 400단계에 걸려 있던 Bypass(절대 보존) 로직 때문에 라이트 모드용 고채도 브랜드 컬러가 강제 주입되어 컬러 스케일의 시각적 흐름이 붕괴되는 현상이 발견되었습니다.
  * **Base Color 누적 왜곡 루프**: 탭 이동 및 렌더링 시, 왼쪽의 오리지널 `BASE COLOR` 프리뷰 영역과 URL 쿼리에 사용자가 입력한 오리지널 값(`state.baseHex`)이 아닌 각 모드의 연산 결과물(500단계/400단계 칩 색상)이 역주입되어, 탭 스위칭을 반복할수록 원래 색상이 감쇄되어 탁해지는 무한 루프 결함이 발견되었습니다.
  * **Chroma Easing 리셋 현상**: 프리셋 적용 시 버튼 선택자 클래스 충돌(`.shifting-modes .btn-mode`)로 인해 Easing 모드 스타일이 무조건 Standard로 덮어씌워지고, 페이지 리로드 및 GNB 이동 시 Easing 상태가 보존되지 않고 무조건 Standard로 강제 리셋되는 사용성 결함이 있었습니다.
* **해결 사양**:
  * **다크 400단계 Bypass 해제**: 다크 모드 400단계에 대한 예외 분기를 삭제하여, 400단계 역시 채도 25% 감쇄 연산 결과를 정상 적용받아 주변 단계와 자연스럽게 어우러지도록 핫픽스를 적용했습니다. (라이트 모드 500단계의 브랜드 컬러 보존 Bypass는 유지)
  * **Base Color 뷰 및 상태 격리**: 왼쪽 프리뷰 카드 렌더링 및 `persistBaseColorState` 데이터 동기화 시, 연산 결과가 아닌 오리지널 `state.baseHex`를 직접 사용하도록 소스를 일원화하여 무한 왜곡 루프를 원천 차단했습니다.
  * **Chroma Easing 선택자 격리 및 상태 보존(Persistence)**: 프리셋 적용 시의 갱신 대상을 `#hue-shifting-modes .btn-mode`로 정밀 제한하여 스타일 꼬임을 격리하고, `&easing=` URL 쿼리 파라미터 및 LocalStorage 연동을 통해 페이지 리로드/탭 이동 시에도 사용자가 선택한 채도 Easing 모드(Subtle/Standard)가 그대로 기억되도록 상태 보존 아키텍처를 이식했습니다.
* **적용 파일**: `index.html`, `light.html`, `dark.html` 3종 전체의 핫픽스 및 데이터 동기화 보완.

